[phases.setup]
nixPkgs = ['php82', 'php82Extensions.mysqli', 'php82Extensions.intl', 'php82Extensions.mbstring']

[phases.build]
cmds = ['composer install --no-dev --optimize-autoloader']

[start]
cmd = 'php spark serve --host=0.0.0.0 --port=$PORT'

